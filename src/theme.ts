import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette:{
      primary:{
        main: '#f2b144',
        light: '#FAEEDA',
        dark: '#DAE6FA',
        contrastText: '#fff',
      }

  }
})

export default theme


