import { createTheme } from "@mui/material/styles";

const theme = createTheme({
    palette:{
      primary:{
        main: '#76bcff',
        light: '#fff',
        dark: '#2896ff',
        contrastText: '#383838',
      },
  },

})

export default theme


